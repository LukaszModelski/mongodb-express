import { StyleSheet } from "react-native";

export const splashViewStyles = StyleSheet.create({
  container: {
    display: "flex",
    flexGrow: 1,
    alignItems: "center",
    justifyContent: "center"
  },
});
